#include <canBusHandler.h>

canBusHandler::canBusHandler(char* can_interface) {
	//Inicialização de varáveis desconhecidas para já
	//Guardar os valores necessários para a conexão, nomeadamente as sockets e assim
}